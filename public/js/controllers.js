(function(angular){
  var app = angular.module("CatControllers",[]); //you can define module dependencies also here if needed

  app.controller("CatsCtrl",function(){

  }).controller("CatCtrl",function(){
    
  })
}(window.angular));
